# local 설정
spring:
  config:
    activate:
      on-profile: "db-local"
  redis:
    session:
      host: localhost
      port: 6379
    cache:
      host: localhost
      port: 6379

---
# test 설정
spring:
  config:
    activate:
      on-profile: "db-test"
  redis:
    session:
      host: localhost
      port: 6379
    cache:
      host: localhost
      port: 6379

---
# prod 설정
spring:
  config:
    activate:
      on-profile: "db-prod"
  redis:
    session:
      host: 49.50.164.244
      port: 6380
    cache:
      host: 49.50.164.244
      port: 6379